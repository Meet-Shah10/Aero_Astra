# AERO-ASTRA backend — pinned to major versions verified working during the
# 2026-09-03 audit. No requirements file existed before this; install order
# matters less than making sure everyone's laptop has the same set.

# Core / simulator
numpy
pandas
pydantic>=2

# SENTINEL (ML)
scikit-learn
xgboost
torch
joblib
tqdm

# SHERLOCK / ATHENA (LLM agents)
openai

# SHERLOCK graph
networkx

# Explainability (SHAP plots referenced in backend/results)
shap
matplotlib

# Phase 1 bridge (backend/api.py) — exists now, see roadmap.md §3
fastapi
uvicorn[standard]
websockets
python-dotenv

# SCRIBE runbook templating — not yet built, needed once it is
jinja2

# ATHENA RAG pipeline (chromadb vector store + PDF ingestion + embeddings)
chromadb>=0.5.0
pymupdf
pypdf
langchain
langchain-text-splitters
langchain-community
sentence-transformers

# Tests
pytest
